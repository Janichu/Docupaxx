import { Message } from "semantic-ui-react";

/**
 * QuizMessage
 */
export const QuizMessage = () => {
    return (
        <Message attached textAlign="left" header="Find an Organization!" />
    )
}